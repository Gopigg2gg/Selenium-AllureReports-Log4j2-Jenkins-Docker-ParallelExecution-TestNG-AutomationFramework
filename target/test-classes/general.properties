# target execution: local or grid
target = local

# Prod URL
url.base = 

# global test timeout
timeout = 3

# javafaker locale
faker.locale = pt-BR

# headless mode only for chrome or firefox and local execution
headless = false